@model Project_63132986.Models.ServiceImage

@{
    ViewBag.Title = "Services";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="main-content">
    <section class="section">
        <div class="section-header">
            <h1>Service Image Galleries</h1>
            <div class="section-header-breadcrumb">
                <div class="breadcrumb-item active"><a href="#">Dashboard</a></div>
                <div class="breadcrumb-item"><a href="#">Forms</a></div>
                <div class="breadcrumb-item">Service Image Galleries</div>
            </div>
        </div>

        <div class="section-body">
            <h2 class="section-title">Add Image Into Service Image Gallery</h2>
            <p class="section-lead">We provide advanced input fields, such as date picker, color picker, and so on.</p>

            <div class="row">
                <div class="col-12 col-md-12 col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <h4>Input Text</h4>
                        </div>
                        @using (Html.BeginForm("Create", "ServiceImages_63132986", FormMethod.Post, new { enctype = "multipart/form-data" }))
                        {
                            @Html.AntiForgeryToken()
                            <div class="card-body">
                                @*Image*@
                                <div class="form-group">

                                    <input value="@ViewBag.ServiceID" name="ServiceID" type="hidden" class="form-control">
                                </div>
                                <div class="form-group">
                                    <label>Image</label>
                                    <input name="Image" type="file" class="form-control">
                                </div>
                                <div>
                                    <a href="/ServiceImages_63132986/Index/@ViewBag.ServiceID" style="width:70px" class="text-white btn btn-danger"><i class="bi bi-escape"></i></a>
                                    &emsp;<button class="btn btn-primary">Create</button>
                                </div>
                            </div>
                        }

                    </div>


                </div>

            </div>
        </div>
    </section>
</div>